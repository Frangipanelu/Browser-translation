# 划词翻译助手 v2.0.0 — 产品分析与改进报告

> 分析日期：2026-07-30  
> 版本：v1.0.0 → v2.0.0

---

## 一、问题诊断与修复总结

### 问题 1：无法识别中译英 / 英译中，VPN 开启也无法翻译

**根因分析：**

| 层面 | 具体问题 |
|------|---------|
| 语言检测 | `detectLang()` 阈值设为 `cjk/total > 0.3`，但未统计数字、标点等"其他"字符。选中"Hello 2024！" 时 cjk=0、latin=5、other=2，判定为英文（正确），但选中"你好 world" 时 cjk=2、latin=5，比例 2/7≈0.286 < 0.3，会误判为英文→中文（实际应为中文→英文） |
| 源语言硬编码 | `background.js` 中 `sl = sourceLang === 'zh-CN' ? 'zh-CN' : sourceLang`，一旦本地误判，就把错误的源语言传给 Google，导致翻译结果不可控 |
| 单点故障 | 翻译只走 Google 一个引擎。Google 免费接口（`client=gtx`）不稳定，常返回 429 或被限流，且国内需翻墙。VPN 开启时如果 DNS 没走代理，`translate.googleapis.com` 仍可能解析失败 |
| CSP 拦截 | 词典查询 `api.dictionaryapi.dev` 在 content script 中直接 `fetch`，部分网站（GitHub、银行等）的 CSP 策略会拦截该请求，导致音标和释义不显示 |

**v2.0 修复方案：**

1. **Google 始终用 `sl=auto`**：不再将本地检测结果传给 Google，让 Google 自己做语言检测，准确率远高于本地字符统计
2. **多引擎备用机制**：Google → MyMemory 自动降级。Google 失败时自动切换到 MyMemory 免费翻译 API（无需 Key，每月 5000 词额度）
3. **语言检测改进**：阈值从 0.3 降到 0.2，同时统计"其他"字符，减少误判
4. **手动切换方向**：气泡内新增 🔄 按钮，用户可一键翻转翻译方向（中→英 ⇄ 英→中），解决自动检测不准的问题
5. **词典查询归集到 background**：所有外部 API 调用统一走 Service Worker，彻底避免页面 CSP 拦截
6. **连接测试功能**：设置页面新增"测试翻译连接"按钮，一键检测 Google / MyMemory 是否可用，给出明确反馈
7. **AbortSignal.timeout**：所有 fetch 添加 8 秒超时，避免 VPN 异常时请求无限挂起
8. **详细错误提示**：翻译失败时显示具体原因（网络/VPN/文字过短）和重试按钮

---

### 问题 2：Obsidian 同步失效

**根因分析：**

| 层面 | 具体问题 |
|------|---------|
| URI 长度限制 | 原 8000 字符限制过低。单词本超过 ~60 个单词时，编码后的 URI 就超长，直接返回 `false` 静默失败，用户看到"已导出 1/1"但实际什么都没发生 |
| 关闭页面不同步 | `beforeunload` 事件只弹了一个原生确认框，**没有任何实际同步操作**。用户点了"留在此页"后也不知道该做什么 |
| 无变更追踪 | 没有记录单词本是否被修改过，每次关闭都弹确认框（即使用户只是查看了一下） |
| 导出结果误导 | `exportToObsidian` 返回 `{ exported: 1, total: 1 }` 但实际只写了 1 个文件，显示"已导出 1/1 个标签分组"让人以为只导出了 1 个单词 |
| URI 在后台打开 | `active: true` 会把 Obsidian 标签页切到前台，打断用户操作 |
| 等待时间过长 | 导出后标签页 8 秒才关闭，影响体验 |

**v2.0 修复方案：**

1. **变更追踪机制**：新增 `needsSync` 标记。每次添加/删除单词时自动标记，同步成功后清除。只有标记为"未同步"时才在关闭页面时提示
2. **同步提示横幅**：单词本页面顶部显示黄色横幅"有未同步的变更 → [立即同步]"，点击即可一键导出
3. **URI 长度提升到 100KB**：Advanced URI 插件对长 URL 支持较好，提升到 100KB 可支持 ~500 个单词一次性写入
4. **失败自动降级**：URI 方式失败时（URI 过长 / Obsidian 未运行 / 插件未安装），自动下载 .md 文件并提示用户手动拖入
5. **导出反馈改进**：显示"已增量同步导出 85 个单词"而非误导性的"已导出 1/1 个标签分组"
6. **标签页后台静默**：`active: false` 不打断用户，3 秒后自动关闭
7. **同步后清除标记**：成功同步后自动清除 `needsSync` 标记，不再重复弹窗

---

### 问题 3：开发者模式警告

**根因分析：**

Chrome / Edge 在开发者模式下加载未打包扩展时，每次启动浏览器都会弹出警告：
> "停用开发者模式扩展程序 — 以开发者模式运行的扩展程序可能损害你的计算机"

这是浏览器内置的安全机制，**不是扩展代码的问题**，任何以开发者模式加载的扩展都会触发。

**解决方案（随附工具）：**

| 文件 | 作用 |
|------|------|
| `pack-extension.ps1` | 自动调用浏览器 CLI 将扩展打包为 .crx 文件 |
| `install-policy.reg` | 添加 Windows 注册表策略，白名单允许所有扩展安装，消除警告 |
| `uninstall-policy.reg` | 移除策略（如需恢复） |

**使用步骤：**
1. 运行 `pack-extension.ps1`（或手动在 `chrome://extensions` 点"打包扩展程序"）
2. 将生成的 `.crx` 拖入 `chrome://extensions` 安装
3. 双击 `install-policy.reg` 合并注册表
4. 重启浏览器，警告不再出现

> 注意：`.crx` 安装方式在部分 Chrome 版本可能被限制，此时仍需用"加载已解压的扩展程序"方式加载，但注册表策略会消除警告。

---

## 二、架构分析与改进建议

### 当前架构

```
┌─────────────┐     ┌──────────────────┐     ┌───────────────┐
│ content.js  │────▶│  background.js   │────▶│ Google API    │
│ (页面注入)   │     │ (Service Worker)  │     │ Dictionary API│
│ 划词+气泡    │◀────│ 翻译+存储+导出    │◀────│ MyMemory API │
└─────────────┘     └──────────────────┘     └───────────────┘
```

### 架构优点
- Manifest V3 规范，符合 Chrome 最新要求
- Service Worker 架构轻量，不常驻内存
- chrome.storage.local 持久化，无需后端服务器

### 架构缺陷与改进方向

#### 1. 数据安全风险（高优先级）

**问题：** 单词数据仅存在 `chrome.storage.local`，浏览器卸载、扩展删除、Profile 切换都会导致数据丢失。

**建议：**
- 添加自动定时导出（每周自动下载 .md 备份到 Downloads）
- 考虑接入 chrome.sync API 同步到 Google 账号
- 单词本页面显示"最后备份时间"，超 7 天未备份时提示

#### 2. 翻译质量局限（中优先级）

**问题：** Google 免费接口返回的是机翻结果，无上下文优化。中译英时如果输入是成语或专业术语，翻译质量较低。

**建议：**
- 接入百度翻译 API（免费额度 20000 字/月，国内可直接访问，无需 VPN）
- 接入有道翻译 API（免费额度，中英互译质量更好）
- 在气泡中显示多引擎结果对比（可选开关）
- 中译英时同时显示词典释义和机翻结果

#### 3. 用户体验改进（中优先级）

**问题：**
- 翻译气泡固定 280px 宽，长文本翻译被截断
- 无暗色模式适配，在暗色网站上半透明背景看不清
- 无法翻译输入框内的文字（只检测 selection，不检测 input/textarea 选中文本）
- 快捷键 `Ctrl+Shift+W` 与部分浏览器/IDE 冲突

**建议：**
- 气泡宽度自适应（最小 280px，最大 400px）
- 添加暗色模式（检测 `prefers-color-scheme: dark`）
- 支持 input/textarea 内划词翻译
- 快捷键改为 `Alt+T` 或可自定义

#### 4. Obsidian 同步深度不足（中优先级）

**问题：** 当前是"一次性导出全部"模式，每次覆盖整个单词本文件。如果用户在 Obsidian 中手动编辑了笔记，会被覆盖。

**建议：**
- 改为增量同步：只写入新增/修改的单词，而非全量覆盖
- 支持 Obsidian Local REST API 插件：通过 HTTP 直接写入文件，无 URI 长度限制
- 支持按标签分别同步到不同笔记文件
- 添加同步历史记录（上次同步时间、同步了多少条）

#### 5. 安全性（低优先级但重要）

**问题：**
- `host_permissions` 包含 `<all_urls>`，请求范围过大
- DeepL API Key 明文存储在 chrome.storage.local 中
- 翻译内容通过 URL 传递（Google Translate API），可能被 ISP/代理记录

**建议：**
- 收窄 `host_permissions` 到实际需要的域名
- API Key 使用 chrome.storage.session 或加密存储
- 文档中说明数据流向（哪些内容发送到哪些服务器）

#### 6. 可维护性（低优先级）

**问题：**
- 所有逻辑在一个大文件中（background.js 300+ 行），没有模块化
- 无 TypeScript 类型定义
- 无自动化测试
- 错误日志使用 `console.error`，用户无法查看

**建议：**
- 拆分模块：`translator.js` / `wordbook.js` / `obsidian.js` / `dictionary.js`
- 添加 JSDoc 类型注释
- 添加简单的单元测试
- 扩展弹窗添加"查看日志"入口

---

## 三、功能路线图建议

### v2.1（近期）
- [ ] 接入百度翻译 API（国内直连，无需 VPN）
- [ ] 暗色模式适配
- [ ] 气泡宽度自适应
- [ ] 输入框内划词翻译
- [ ] 自动备份提醒

### v2.2（中期）
- [ ] Obsidian 增量同步（Local REST API 插件方式）
- [ ] 多引擎结果对比
- [ ] 翻译历史记录
- [ ] 单词复习功能（间隔重复算法）
- [ ] 自定义快捷键

### v3.0（远期）
- [ ] 支持更多语言（日、韩、法、德）
- [ ] PDF 翻译（读取 PDF 选中文本）
- [ ] 划词翻译 API 服务自建选项
- [ ] 端到端加密同步（无需第三方）
- [ ] 语音翻译（语音输入 → 翻译 → TTS 输出）

---

## 四、v2.0 变更清单

| 文件 | 变更 |
|------|------|
| `manifest.json` | 版本升至 2.0.0，新增 MyMemory API 权限 |
| `background.js` | 全部重写：多引擎备用、词典归集、连接测试、同步追踪、改进 Obsidian 导出 |
| `content.js` | 全部重写：改进语言检测、手动切换方向、词典走 background、详细错误提示、重试按钮 |
| `content.css` | 新增切换方向按钮样式、错误提示样式、警告提示样式 |
| `wordbook.js` | 全部重写：同步追踪机制、同步横幅、连接测试、改进导出反馈、降级文件下载 |
| `wordbook.css` | 新增同步横幅、连接测试样式 |
| `wordbook.html` | 新增连接测试按钮，版本号更新，说明文案改进 |
| `pack-extension.ps1` | 新增：自动打包 .crx 脚本 |
| `install-policy.reg` | 新增：注册表策略消除开发者模式警告 |
| `uninstall-policy.reg` | 新增：卸载策略 |
| `README.md` | 更新为 v2.0.0 |

---

## 五、快速验证清单

安装 v2.0 后请逐项验证：

- [ ] 选中中文 → 气泡显示"中→英"，翻译正确
- [ ] 选中英文 → 气泡显示"英→中"，翻译正确
- [ ] 点击 🔄 按钮 → 方向翻转，重新翻译
- [ ] 关闭 VPN → 翻译失败 → 应显示 MyMemory 备用结果或详细错误提示
- [ ] 开启 VPN → Google 翻译正常工作
- [ ] 设置页面 → 点击"测试翻译连接" → 显示可用引擎
- [ ] 收藏单词 → 单词本页面顶部出现"未同步"横幅
- [ ] 点击"立即同步" → Obsidian 打开笔记，内容正确
- [ ] 关闭单词本页面（有未同步变更）→ 弹出确认提示
- [ ] 关闭单词本页面（无未同步变更）→ 不弹提示
- [ ] 单词本为空时导出 → 显示"单词本为空"
- [ ] 浏览器重启后 → 不再显示开发者模式警告（需先配置注册表策略）
